export type SavingThrows = {
    paralyzationPoisonDeath: number; // PPD
    rodStaffWand: number;            // RSW
    petrificationPolymorph: number;  // PP
    breathWeapon: number;            // BW
    spell: number;                   // Spell
}